继续 a-top10 的 Top10 V3 主线工作，但主语境已从“V3 开发中”切换为“V3 已收口，进入正式运行 / 验收 / 定向优化阶段”。

---

# 当前主线状态

## 一、V3 主线已完成收口的部分

### 1）step3_strength_score.py
已完成 V3 收口：

- 区分 `limit_strength_raw` 与 `StrengthScore`
- 清除 merge 脏列污染
- 主列收口完成
- 输出 `strength_feature_count / strength_missing_fields / strength_quality_flag`

判定：已收口，可稳定作为下游输入。

---

### 2）step4_theme_boost.py
已完成 V3 收口：

- 输出 `theme_main / theme_count / theme_heat / board_rank / lhb_flag / lhb_net_buy / north_money_flag`
- 输出 `ThemeBoost`
- 输出 `theme_feature_count / theme_missing_fields / theme_quality_flag`

`debug_step4_theme_20260313.json` 已验收通过。

判定：已收口，可稳定进入 Step5。

---

### 3）step5_ml_probability.py
已完成 V3 主概率引擎收口：

- 输出 `prob_lr / prob_lgbm / prob_rule / Probability / _prob_src`
- `Probability` 成为主概率轴
- `_prob_src` 可追踪最终来源
- 训练接口保留，但训练样本口径已对齐 Step7
- Step5 已不再越权主写 `feature_history.csv`

判定：已收口，当前属于正式 V3 主概率引擎。

---

### 4）step7_self_learning.py
已完成 V3 主体收口并打通：

- 写回 `is_sample_mature / mature_reason / label_delay_flag`
- 写回 `y_limit_hit / y_next_ret`
- 写回 `learnable_flag / reject_reason / sample_quality_grade / batch_quality_score / gate_version / label_version / verify_date`
- `step7_report_latest.json / md` 已稳定产出
- `step7_hit_rate_history.csv` 已稳定产出
- 训练放行链已打通
- `models/step5_lr.joblib / step5_lgbm.joblib` 已真实更新过

判定：已收口，当前是正式 Step7 V3 学习闭环。

---

### 5）feature_history.csv
已完成从旧过程表到 V3 正式样本主干表的升级：

- 已不再是旧 V2 过程表
- 已成为 V3 样本契约主干表
- 旧 `missing_prob_src` 历史遗留样本已通过补历史大幅消化
- 当前 `reject_reason_summary` 已证明：
  - 可学习样本为主
  - 剩余拒绝样本主要为 `pending_next_snapshot`
- 说明当前残留已主要是“最新样本尚未成熟”的自然时间问题，不再是系统结构问题

当前状态可定义为：

**Top10 V3 正式样本主干表**

---

### 6）writers.py
已完成两条主线收口：

#### A. 样本写口收口
- `feature_history.csv` 列契约已锁定
- 旧 V2 残留概率字段已从正式契约中降级 / 清出
- 去重主键与 last-write-wins 策略已建立

#### B. 报告写口收口
当前预测报告已重构为 4 张表：

1. Top10 预测表（明天押谁）
2. 候选池补充表（Top10 之外）
3. 上一交易日预测命中情况
4. 近10日 Top10 绩效 + 近10日整体统计

并已完成以下收口：

- 标题去掉 `《》`
- 字段名人类化（如 `Probability -> 涨停概率`，`_prob_src -> 概率来源`）
- `近10日 Top10 绩效` 已统一以 Step7 正式口径为准
- 增加 `预测日 / 验证日`
- `当日涨停家数` 已可按历史 `verify_date` 动态读取

判定：报告层主线已基本收口。

---

## 二、当前主线总判断

### Top10 V3 已可正式判定为：
# Top10 V3 主线收口完成

后续不再以“V3 还没开发完”的语境继续推进。  
从现在开始，主语境切换为：

# 正式运行 + 持续验收 + 定向优化

---

# 当前遗留问题

当前已不存在阻断 V3 成立的主干问题。  
剩余问题全部视为：

## V3+ 优化项
而不是 “V3 主线未完成”。

主要包括：

### 1）报告层仍可继续优化
例如：

- 第三张表 / 第四张表排版精修
- 百分比、小数位、列宽等继续收口
- 预测报告的人类阅读体验继续提升

### 2）统计层仍可继续增强
例如：

- 近10日绩效加入更多收益统计
- 增加中位涨跌幅 / 平均涨跌幅 / 胜率
- 扩展更多滚动窗口统计（20日、40日等）

### 3）模型层仍可继续增强
例如：

- Step5 融合逻辑进一步优化
- LGBM / LR 参数与样本窗口继续调优
- `_prob_src` 结构性分布继续观察

### 4）样本质量层仍可继续增强
例如：

- `reject_reason` 继续细化
- `sample_quality_grade` 继续精修
- 批门阈值继续校准

---

# 下一阶段主线

从现在开始，a-top10 不再进入“大改骨架”模式。  
下一阶段唯一正确主线是：

# 运行验收 + 表现观察 + 定向优化

即：

1. 继续正常手动 / 自动运行
2. 看 `step7_report_latest.json/md`
3. 看 `predict_top10_YYYYMMDD.md`
4. 看 `feature_history.csv`
5. 看 `models/`
6. 发现明确问题后，再做单点优化
7. 不再回到 V2 兼容式修补语境

---

# 下一步文件优先级

如果后续继续开发，只允许按以下顺序处理：

## 第一优先：writers.py
仅在报告展示、绩效表、字段人类化、统计口径上继续单点优化。

## 第二优先：step7_self_learning.py
仅在拒绝样本治理、统计摘要、质量分层上继续单点优化。

## 第三优先：step5_ml_probability.py
仅在概率融合、模型口径、训练窗口上继续单点优化。

---

# 本轮后的工作模式

后续严格遵守：

## 模式 1
一次只处理一个文件，不发散。

## 模式 2
直接输出完整可覆盖版本，不要片段补丁。

## 模式 3
每次输出必须包含三部分：

- 为什么改这个文件
- 改了什么
- 如何验收

## 模式 4
始终以以下文档为锚点：

- `Top10V3 需求.MD`
- `《FeatureHistory 字段入池映射表 V1》.MD`
- `Top10 V3 前期规划总纲.MD`

但当前若与“V3 已收口”的状态冲突，则以本文件最新状态为准。

---

# 回答风格要求

- 冷峻
- 严谨
- 工程化
- 不泛泛总结
- 不回到 V2 接管式重构语境
- 不再把当前系统表述成“V3 未完成”
- 只在发现明确问题时进入单点优化

---

# 当前一句话任务

Top10 V3 主线已收口。  
后续若继续工作，默认进入：

**正式运行、持续验收、定向优化**

而不是重新进入主线大开发。
