继续 a-top10 的 Top10 V3 主线开发，严格按 V3 总纲推进，不回到 V2 接管式重构语境。

【当前主线状态】

当前 V3 已完成并已验收通过/基本通过的阶段如下：

1. step3_strength_score.py
已完成 V3 收口：
- 区分 limit_strength_raw 与 StrengthScore
- 清除 merge 脏列污染
- 主列收口完成
- 输出 strength_feature_count / strength_missing_fields / strength_quality_flag
- Step3 产物已基本通过验收，可进入下游

2. step4_theme_boost.py
已完成 V3 收口：
- 输出 theme_main / theme_count / theme_heat / board_rank / lhb_flag / lhb_net_buy / north_money_flag
- 输出 ThemeBoost
- 输出 theme_feature_count / theme_missing_fields / theme_quality_flag
- debug_step4_theme_20260313.json 已验收，判定 A-，允许进入下一文件

3. step5_ml_probability.py
已完成 V3 主概率引擎改造：
- 输出 prob_lr / prob_lgbm / prob_rule / Probability / _prob_src
- Probability 成为主概率轴
- _prob_src 可追踪最终来源
- 仍保留训练接口兼容
- 但 Step5 的最终落库收口依赖 Step7 / writers 联合完成

4. step7_self_learning.py
已完成 V3 主体改造并已接通：
- 写回 is_sample_mature / mature_reason / label_delay_flag
- 写回 y_limit_hit / y_next_ret
- 写回 learnable_flag / reject_reason / sample_quality_grade / batch_quality_score / gate_version / label_version / verify_date
- step7_report_latest.json / md 已产出
- step7_hit_rate_history.csv 已产出
- feature_history.csv 已升级为 V3 样本契约主干表

5. feature_history.csv 当前验收结论
已判定：
- 不再是旧 V2 过程表
- 已升级为 V3 样本契约主干表
- 当前评级 A-
- 可继续推进主线

【当前遗留问题】

当前 feature_history.csv 仍存在以下残留，需在下一步收口：

1. 旧 V2 概率字段仍残留：
- prob_ml
- prob_final
- prob_src
- prob_ml_available
- prob_fusion_mode

2. close 列仍未稳定落库

3. y_next_ret 当前多数为空，需结合 close 字段稳定收口

4. reject_reason 在当前展示样本中多数为空，需在 writers/后续验收中继续确认拒绝样本是否真实落出

【下一步唯一主线】

不要再讨论抽象方案，直接进入：

第一优先文件：
a_top10/io/writers.py

【本轮目标】

把 feature_history.csv 彻底收成稳定的 V3 正式样本表。

writers.py 本轮必须完成以下目标：

1. 锁死 feature_history.csv 列契约
至少稳定支持以下字段：

- run_time_utc
- trade_date
- ts_code
- name
- Probability
- _prob_src
- StrengthScore
- ThemeBoost
- seal_amount
- open_times
- turnover_rate
- prob_lr
- prob_lgbm
- prob_rule
- is_sample_mature
- mature_reason
- label_delay_flag
- y_limit_hit
- y_next_ret
- learnable_flag
- reject_reason
- sample_quality_grade
- batch_quality_score
- gate_version
- label_version
- verify_date
- close

2. 清理旧 V2 残留字段
重点处理：
- prob_ml
- prob_final
- prob_src
- prob_ml_available
- prob_fusion_mode

原则：
- 不再作为正式 V3 样本契约字段继续保留
- 若为兼容暂留，也必须降级处理，不得污染正式列契约

3. 固定列顺序
保证历史补跑与实时运行口径一致，不再漂移

4. 固定去重主键
以：
- trade_date + ts_code
作为去重主键，避免重复脏样本

5. 明确 append-only + last-write-wins 策略
保证每天重复跑不会把 feature_history.csv 写乱

6. 把 close 字段正式纳入稳定落库
为 y_next_ret 收口做准备

【本轮工作模式】

后续严格遵守：

模式 1：
一次只处理一个文件，不发散

模式 2：
直接输出完整可覆盖版本，不要片段补丁

模式 3：
每次输出必须包含三部分：
- 为什么改这个文件
- 改了什么
- 如何验收

模式 4：
始终以以下 V3 文档锚点为裁决依据：
- Top10V3 需求.MD
- 《FeatureHistory 字段入池映射表 V1》.MD
- Top10 V3 前期规划总纲.MD

若与旧 V2 逻辑冲突，一律以 V3 文档为准

【回答风格要求】

冷峻
严谨
工程化
不泛泛总结
不回到兼容式修补路线
不反复讨论 V2 是否完成
直接进入 V3 逐文件改造

【当前一句话任务】

直接读取仓库当前 a_top10/io/writers.py 现状，
然后只围绕 V3 目标改这个文件，
输出完整可覆盖版本，
并附简明工程化验收说明。
