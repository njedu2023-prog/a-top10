# 衔接口令_0314

## 当前阶段定位

当前 `a-top10` 已进入 **V2 第一阶段主链重构**，并且已经完成最关键的一刀：

- `step5_ml_probability.py`
- `step6_final_topn.py`
- `configs/default.yml`
- `a_top10/main.py`
- `a_top10/io/writers.py`

这一轮不是停留在方案层，而是已经完成了一次真实落地与跑通。

---

## 当前已完成成果

本轮最核心成果有四条：

### 1. Step5 概率语义已拆分
旧的脏 `Probability` 已拆成：

- `prob_rule`
- `prob_ml`
- `prob_final`

并保留兼容字段：

- `Probability = prob_final`

这意味着旧消费链没断，但内部概率认知已经完成 V2 化。

### 2. Step6 已开始净化为纯终排器
Step6 不再以旧 `Probability` 作为唯一脏主字段，而是正式转向：

- 主概率字段：`prob_final`
- 主终排字段：`final_score`

同时输出：

- Top10
- 全量排序

### 3. `outputs/decisio/` 路径不断链
本轮重构继续遵守硬约束：

- 原有对外产物形式尽量不变
- 原有路径不变
- `outputs/decisio/` 不更名、不断链

并且当前已能生成：

- `pred_decisio_latest.csv`
- `pred_decisio_YYYYMMDD.csv`

### 4. 已经完成一次真实跑通
最新验收对应交易日是：

- 预测日：`20260313`
- 验证目标日：`20260316`

注意：

- `20260314` 不是交易日
- 所以当前最新有效产物仍围绕 `20260313`

---

## 当前验收结论

### 已通过部分

#### A. 主终值链已通过
`outputs/decisio/pred_decisio_latest.csv` 已具备以下字段：

- `prob_rule`
- `prob_ml`
- `prob_final`
- `Probability`
- `final_score`
- `score`
- `StrengthScore`
- `ThemeBoost`

并且不是只有 Top10，而是全量排序结果。

这说明：

- Step5 V2 概率语义已进入正式产物
- Step6 V2 终排语义已进入正式产物
- 第一阶段最危险主链已经被切开并跑通

#### B. 学习链未断
以下文件仍正常生成：

- `outputs/learning/pred_top10_latest.csv`
- `outputs/learning/pred_top10_20260313.csv`
- `outputs/learning/pred_top10_history.csv`
- `outputs/learning/feature_history.csv`

其中 `feature_history.csv` 已开始带有 V2 概率字段，说明 Step5 新语义已经真实落库，不只是内存里改了。

#### C. 报告链未断
以下文件仍正常生成：

- `outputs/predict_top10_20260313.md`
- `outputs/predict_top10_20260313.json`
- `outputs/latest.md`

说明对外展示层还活着，没有被第一阶段重构打断。

---

## 当前还没彻底完成的地方

虽然第一阶段关键主链已经跑通，但还没有彻底收口，仍有两点没完全统一：

### 1. learning 侧 Top10 写出 schema 仍偏旧
例如：

- `pred_top10_latest.csv`
- `pred_top10_YYYYMMDD.csv`

目前仍偏向旧字段口径，未完全统一到：

- `prob_final`
- `final_score`

也就是说：

- `decisio` 主终值链已经先进入 V2
- `learning` 侧写出层还处于兼容过渡状态

### 2. 第二阶段尚未开始
当前还没有正式进入：

- `step2_candidate_pool.py`
- `step7_self_learning.py`

所以当前系统状态是：

> 主终值内核已经开始 V2 化，但候选池上限与学习闭环污染问题，还没有开始真正重构。

---

## 当前系统状态的准确判断

现在的 `a-top10` 不再是原来那种“旧链硬跑”的状态。

更准确地说：

> 当前系统已经变成“旧外壳仍兼容、但 Step5 + Step6 主终值内核开始 V2 化”的状态。

也就是：

- 外部路径还在
- 外部消费链还在
- 旧字段还保留
- 但内部最关键的概率与终排语义，已经不是旧逻辑了

---

## 下次继续开发的主线

下次继续时，不要回头重新讨论抽象方案，也不要发散去做别的支线。

直接按下面顺序推进。

### 第一步：收掉第一阶段最后一截
先继续处理 **learning 侧写出统一**。

目标是检查并必要时继续调整：

- `outputs/learning/pred_top10_latest.csv`
- `outputs/learning/pred_top10_YYYYMMDD.csv`
- `outputs/learning/pred_top10_history.csv`

目标不是改路径，而是尽量把 learning 侧产物也统一到 V2 主语义，至少逐步显式吃到：

- `prob_final`
- `final_score`

这一刀收干净后，第一阶段才算更完整。

### 第二步：切第二阶段主线
第一阶段收口后，下一条主线应立即切到：

- `a_top10/steps/step2_candidate_pool.py`
- `a_top10/steps/step7_self_learning.py`

原因：

- Step2 决定候选池上限
- Step7 决定系统是否继续固化旧偏差

也就是说：

- 第一阶段解决“终值污染”
- 第二阶段解决“候选池上限 + 学习闭环污染”

---

## 下次继续开发时的直接执行口令

下次继续时，直接这样开始：

> 继续 `a-top10 V2` 重构，不再讨论抽象方案。先收掉第一阶段最后一截，检查并统一 `outputs/learning/` 相关写出 schema，重点关注 `pred_top10_latest.csv`、`pred_top10_YYYYMMDD.csv`、`pred_top10_history.csv` 是否已经显式对齐 `prob_final / final_score` 主语义。在不破坏旧路径、旧文件名、旧消费链的前提下，把第一阶段真正收口。收口完成后，立刻切入第二阶段，开始重构 `step2_candidate_pool.py` 和 `step7_self_learning.py`。

---

## 一句话总结当前锚点

**当前 `a-top10` 已完成 V2 第一阶段最关键的主终值重构，`Step5 + Step6` 已经不是旧逻辑，旧壳仍兼容，但内核已开始换代。下次继续，不发散，先收第一阶段尾巴，再切第二阶段。**
