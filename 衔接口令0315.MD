# 继续 a-top10 的 Top10 V3 主线开发，严格以 V3 总纲推进，不再回到 V2 接管式重构语境。

## 【当前主线锚点】

当前仓库内已形成以下 V3 文档锚点，后续开发必须以它们为准，不再回退到旧的 V2 讨论口径：

1. `Top10V3 需求.MD`
2. `《FeatureHistory 字段入池映射表 V1》.MD`
3. `Top10 V3 前期规划总纲.MD`

其中：

- `Top10V3 需求.MD`：定义 V3 的系统目标与升级方向
- `《FeatureHistory 字段入池映射表 V1》.MD`：定义训练样本字段契约
- `Top10 V3 前期规划总纲.MD`：定义 V3 的质量闸门、标签成熟、排序目标、数据修复优先级、阶段路线图

这三份文档已经构成当前 V3 的正式规划层锚点。

---

## 【总原则】

1. 不要再回到“V2 是否跑通”的旧主线。
2. 不要再围绕旧兼容、旧链路、旧 write-once 壳层做抽象讨论。
3. 后续所有开发，以 **V3 总纲落代码** 为唯一主线。
4. 现在的核心问题，不再是“主链是否接通”，而是：
   - V3 字段契约是否真实落库
   - Step7 是否真实执行样本成熟与质量闸门
   - Step6 是否真实执行 RankScore 排序
   - feature_history 是否从流水表升级为正式训练样本表
5. 开发顺序必须受控，不允许多文件同时发散乱改。

---

## 【当前阶段判断】

当前阶段不是继续扩文档，而是进入：

> **《Top10 V3 前期规划总纲》对应的代码落地阶段**

即，从规划层切到执行层。

当前最重要的目标不是“继续讨论方案”，而是把总纲中的规则压到真实代码文件里。

---

## 【后续开发固定顺序】

后续严格按以下顺序推进，不得跳步：

### 第一步
改 `a_top10/steps/step3_strength_score.py`

目标：

- 明确 StrengthScore 的输入字段
- 区分 `limit_strength_raw` 与 `StrengthScore`
- 严格执行 `open_times / seal_amount / turnover_rate` 的缺失策略
- 为下游写出可落库的强度字段

### 第二步
改 `a_top10/steps/step4_theme_boost.py`

目标：

- 明确 ThemeBoost 的输入字段
- 输出 `ThemeBoost`
- 输出题材层质量辅助字段

### 第三步
改 `a_top10/steps/step5_ml_probability.py`

目标：

- 输出 `prob_lr / prob_lgbm / prob_rule / Probability / _prob_src`
- 让 Probability 成为主排序轴
- 让概率来源可追踪

### 第四步
改 `a_top10/steps/step7_self_learning.py`

目标：

- 真实生成 `is_sample_mature`
- 真实生成 `y_limit_hit / y_next_ret`
- 真实执行样本级闸门
- 真实执行批级闸门
- 输出 `learnable_flag / reject_reason / batch_quality_score`

### 第五步
改 `a_top10/io/writers.py`

目标：

- 让 `feature_history.csv` 真正落 V3 契约字段
- 保证列结构稳定
- 保证历史补跑与实时运行同口径

### 第六步
改 `a_top10/steps/step6_final_topn.py`

目标：

- 引入 `RankScore`
- 让最终 Top10 排序从“简单概率排序”升级为“显式综合排序”

### 第七步
最后收口 `a_top10/main.py` 与 `configs/default.yml`

目标：

- 固定 V3 主链顺序
- 固定版本字段
- 固定质量闸门参数
- 固定 RankScore 权重来源

---

## 【当前对话的固定工作模式】

后续新对话不要重新讨论抽象规划，直接进入执行：

### 模式 1：先锁一个文件
一次只处理一个文件，不发散。

### 模式 2：直接输出完整可覆盖版本
不要片段式补丁为主，不要零散修改建议。

### 模式 3：每次输出必须包含三部分
1. 为什么改这个文件
2. 改了什么
3. 如何验收

### 模式 4：始终以 V3 文档锚点为裁决依据
如与旧 V2 逻辑冲突，以 V3 文档为准。

---

## 【当前第一任务】

新对话开启后，不要再讨论抽象方案，直接执行：

> **第一优先文件：`a_top10/steps/step3_strength_score.py`**

执行要求：

1. 先基于仓库当前文件状态阅读并判断现状
2. 只围绕 V3 目标修改该文件
3. 直接输出完整可覆盖版本
4. 输出后附上简明工程化验收说明

---

## 【回答风格要求】

- 冷峻
- 严谨
- 工程化
- 不泛泛总结
- 不回到兼容式修补路线
- 不再反复讨论 V2 是否完成
- 直接从 V3 总纲进入逐文件改造

---

## 【一句话总结】

> 从现在开始，a-top10 的主线不再是 V2 接管式重构，而是 **Top10 V3 总纲落代码**；第一站，直接改 `step3_strength_score.py`。
