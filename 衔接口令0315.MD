继续 a-top10 的 V2 接管式重构与验收，沿用以下已确认状态与规则，不要回到旧链路兼容思路。

【总原则】
1. 旧文件名保留，旧路径保留。
2. 旧代码、旧逻辑、旧兼容、旧 fallback 全部不要。
3. a-top10 内核只允许是 V2。
4. 可以接受旧链路中断，但 V2 跑通后必须无缝接管旧产物出口，保证下游消费不受影响。
5. 保留的是旧产物外壳，不保留旧算法内核。
6. 所有文件只按一个标准保留：是否服务 V2 主链。不是就删。

【本轮已完成】
1. 已对以下文件做 V2 硬切重构：
- a_top10/io/writers.py
- a_top10/main.py
- a_top10/steps/step7_self_learning.py
- a_top10/steps/step2_candidate_pool.py

2. 已连续修过 writers.py 的三个真实问题：
- 修掉 DataFrame 参与 `or` 布尔判断导致的报错
- 把 pred_top10_history.csv 的重建判定从“完全等列顺序一致”改为“V2 必需列存在即可”
- 把 outputs/learning/pred_top10_{trade_date}.csv 改为直接覆盖写，不再 write-once，避免旧脏 dated 文件钉死 V2 history

【当前运行结果】
最新自动运行已经跑通主链并产出 Step7 报告：
- outputs/learning/step7_report_latest.md 已更新
- 报告显示：
  - LatestSnapshot = 20260313
  - LabelUpperBound = 20260313
  - 最新命中：trade_date=20260312 -> actual_next_trade_date=20260313
  - 近10日 Top10 命中率表已正常生成
  - 训练数据概况已正常统计
  - train_rows=2058, pos/neg=368/1690
  - trained=False, reason=skip_train: quality_gate_fail
  - warnings 里有：
    - label_pending: trade_date=20260313 no next snapshot
    - skip_train: quality_gate_fail

这说明：
1. Step2 -> Step6 -> writers -> Step7 主链已经打通。
2. pred_top10_history.csv 已不再是空表，否则 Step7 不会生成当前这份报告。
3. 当前主线已从“修主链断裂”转入“收壳层一致性 + 做正式验收”。

【当前判断】
1. latest.md 已更新，说明 latest 出口活了。
2. predict_top10_20260313.md 如果没被覆盖，属于旧 write-once 壳层一致性问题，不是主链致命问题。
3. Step7 命中统计链已经真实运行，不要再怀疑 Step7 是否接上。
4. 现在下一步不要发散到旧链路，而应进入：
- 主产物正式验收
- dated 壳层一致性收口
- 判断是否需要继续清理旧非 V2 产物/代码

【新对话的任务】
不要重新讨论抽象方案，直接进入验收与收口：

第一步：先列出“本轮 V2 主链跑通后的正式验收清单”，按优先级排序。
重点优先检查这些文件是否已经真正符合 V2 接管目标：
- outputs/learning/pred_top10_20260313.csv
- outputs/learning/pred_top10_history.csv
- outputs/learning/pred_top10_latest.csv
- outputs/decisio/pred_decisio_latest.csv
- outputs/learning/step7_hit_rate_history.csv
- outputs/learning/step7_report_latest.json
- outputs/learning/step7_report_latest.md

第二步：明确判断哪些问题仍然存在，但已经从“主链问题”降级为“壳层一致性问题”。
尤其关注：
- predict_top10_YYYYMMDD.md / json 是否还受 write-once 影响
- 是否要让 dated md/json 也改成 V2 覆盖写
- 是否要清理 outputs/learning 目录内旧非 V2 日文件，避免长期污染 history rebuild

第三步：如果发现下一个最合理动作是继续改代码，不要发散，一次只锁定一个文件，直接输出完整可覆盖版本。

回答风格要求：
- 冷峻、严谨、工程化
- 不要复述泛泛总结
- 不要再走兼容式修补路线
- 直接从当前已跑通状态往下推进
